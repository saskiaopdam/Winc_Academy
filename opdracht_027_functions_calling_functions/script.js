const isAdult = function (age) {
  if (age >= 18) {
    return true;
  } else {
    return false;
  }
};

const greet = function (age) {
  isAdult(age);
  consi;
  return "Hello there";
  return "Hey kiddo";
};

greet(16);

// VAT exercise 1
